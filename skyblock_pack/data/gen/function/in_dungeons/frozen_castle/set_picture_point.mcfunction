setblock ~ ~ ~ air

# Сохраняем координаты в хранилище, чтобы после победы телепортировать всех
execute summon marker run function gen:in_dungeons/frozen_castle/store_picture_cords

forceload add ~ ~

